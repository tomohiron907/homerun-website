# ASSETS — LP に必要な素材の仕様

HomeRun の LP で使う画像・動画の仕様書。**素材はすべてユーザー側で用意する**前提。

## 使い方

1. この文書の仕様に沿って素材を作る
2. `public/` の指定パスに置く
3. `src/lib/assets.ts` の対応する行を `null` から実パスに書き換える
4. `npm run dev` で確認 → `npm run build`

`src/lib/assets.ts` が唯一の差し替え箇所。コンポーネントは触らなくてよい。
`null` のままでも **ページは壊れない**（CSS で描いたモックが代わりに出る）ので、
揃った素材から順に投入できる。

## 共通の撮影ルール（全素材に適用）

- **デモ用のクリーンなホームディレクトリを使う。** 実名・取引先名・請求書など、
  個人情報や機密が読み取れるファイル名を絶対に映さない。スクリーンショットは公開物。
- メニューバー・Dock・通知は映さない（HomeRun のウィンドウだけを切り出す）
- デスクトップの壁紙は無地（単色）に
- ライトモード／ダークモードは統一する。LP の製品パネルはダーク基調なので **ダークモード推奨**
- Retina（2x）でキャプチャする
- マウスカーソルは極力映さない・動かさない。「キーボードだけで動く」ことが売りなので、
  カーソルが動くと訴求が壊れる

---

## A. デモ動画 【最重要】

販売ドキュメントが「これが無いと Vim スタイルの価値が伝わらない」と明記している、
LP の中で最も効く素材。

| 項目 | 仕様 |
|---|---|
| ファイル | `public/demo/homerun-demo.mp4`（可能なら `homerun-demo.webm` も） |
| ポスター | `public/demo/homerun-demo-poster.png`（動画の 1 フレーム目相当） |
| 収録 | 2560×1440 |
| 書き出し | 1600×900（16:9）、H.264、30fps |
| 尺 | 15〜25 秒、**シームレスループ**（最後のフレームが最初に自然につながる） |
| 音声 | **なし**（`muted autoplay loop playsinline` で自動再生するため） |
| 容量 | **5 MB 以下**を目標。GitHub Pages 配信なので初回表示速度に直結する |

**映す内容（この順番）**

1. `j` / `k` で上下移動
2. `l` でディレクトリに入る → `h` で戻る
3. `gd` で `~/Documents` にジャンプ
4. `⌘⇧F` でグローバル fuzzy finder → 数文字打って絞り込み → Enter
5. `Space` で Quick Look プレビュー → もう一度 `Space` で閉じる
6. ファイルを他アプリへドラッグアウト

**撮影の注意**

- **打鍵をオンスクリーンで可視化する**（[KeyCastr](https://github.com/keycastr/keycastr) など）。
  何のキーを押しているか見えないと、この動画は「ただのファイル一覧」にしか見えない
- 1 操作ごとに 0.5〜1 秒の間を置く。速すぎると何が起きたか読めない
- 透明ウィンドウの角丸がきれいに出るように、背景は無地に

**未提供のときの見え方**: `Demo` セクションにダークのアプリウィンドウ風モックが出て、
カーソルが 4 行を上下に往復するアニメーションがループする。公開しても破綻はしないが、
訴求力は明確に落ちる。**launch までに用意することを強く推奨。**

---

## B. 機能スクリーンショット ×5

`Features` セクションの 5 ブロックと 1:1 対応する。

| ファイル | 対応する機能 | 映す内容 |
|---|---|---|
| `public/features/01-navigation.png` | Home-row navigation | 通常のファイル一覧。カーソルが 1 行に当たっている状態 |
| `public/features/02-jump.png` | Jump anywhere | `g` を押した直後のジャンプ候補（`h` `w` `d` `c`）が出ている状態 |
| `public/features/03-find.png` | Find, don't browse | `⌘⇧F` のグローバル fuzzy finder に数文字入力し、候補が絞り込まれた状態 |
| `public/features/04-preview.png` | See without leaving | `Space` の Quick Look プレビュー、または `Tab` のサムネイルグリッド |
| `public/features/05-fileops.png` | Do the work in place | `⌘R` のインラインリネーム中、またはコピー進捗ダイアログ |

| 項目 | 仕様 |
|---|---|
| 形式 | PNG |
| 解像度 | 2000×1250（Retina 2x、表示は 1000×625 相当） |
| アスペクト比 | 16:10 |
| 影 | **影なしで撮る**（`⌘⇧4` → Space の後、Option を押しながらクリックで影を外す）。影は LP 側で付ける |

**未提供のときの見え方**: 各ブロックに、その機能に合わせた CSS モックウィンドウが出る
（ジャンプ候補一覧、fuzzy finder の絞り込み、ファイル一覧など）。
レイアウトは崩れないので、5 枚のうち撮れたものから順に差し替えてよい。

---

## C. Actions メニューのスクリーンショット

| 項目 | 仕様 |
|---|---|
| ファイル | `public/features/actions.png` |
| 仕様 | B と同じ（PNG / 2000×1250 / 16:10 / 影なし） |
| 内容 | `⌘⇧X` で開いたアクションメニュー。同梱の 6 スクリプトが並んでいる状態 |

**未提供のときの見え方**: アクションスクリプトのサンプルコード（`backup_to_r2.sh`）が
ダークのコードパネルで表示される。これは意図した見せ方でもあるので、
**優先度は低め**（B や A を先に用意したほうがよい）。

---

## D. ブランド素材

| ファイル | サイズ | 用途 | 備考 |
|---|---|---|---|
| `public/og.png` | 1200×630 | OGP / Twitter Card | SNS シェア時のカード画像。**未提供だと画像なしのカードになる** |
| `src/app/icon.png` | 256×256 | ブラウザタブ | **投入済み**。Next の file convention（`<link rel="icon">` が自動で入る）。`favicon.ico` は不要 |
| `src/app/apple-icon.png` | 180×180 | iOS ホーム画面 | **投入済み**。同じく file convention |

> `public/icon.png` は置かないこと。app-dir 側の `icon.png` と URL (`/icon.png`) が
> 衝突し、静的ホスト上でどちらが配信されるか決まらなくなる。

**`og.png` に入れる内容の推奨**: 白背景に「HomeRun」のワードマーク、
サブコピー "Keyboard-first file browser for macOS"、`h j k l` のキーキャップ。
LP のヒーローと同じトーンにする。SNS のプレビューは小さく表示されるので、
文字は大きく・要素は少なく。

**現状**: `public/og.png` は投入済み。再生成の手順は `scripts/og.md` を参照
（`scripts/og.html` を Chrome ヘッドレスで 1200×630 撮影しているだけ）。
ヒーローの文言を変えたら og も撮り直すこと。

**未提供のときの見え方**: `assets.og` は `/og.png` を指したままなので、
**ファイルが無いと OGP 画像が 404 になる**。他の素材と違い null フォールバックが無いので、
og.png だけは公開前に必ず用意すること（`RELEASE-CHECKLIST.md` にも記載）。

---

## E. 任意（あると良い）

| 素材 | 用途 |
|---|---|
| ワードマーク SVG | Nav / Footer のテキストロゴを画像に置き換える場合 |
| カラムビュー（Miller columns）のスクショ | 機能 04 の 2 枚目として |
| フォルダカラーのスクショ | `MacNative` セクションに追加する場合 |
| ダーク／ライト両方のスクショ | 将来ダークモード対応する場合 |

これらは現在コード側に受け口が無いので、使うならセクションの追加が必要。

---

## 素材が揃ったかのチェック

```bash
# 配置済みファイルの確認
find public -type f | sort

# 差し替え漏れの確認（null が残っていないか）
grep -n "null" src/lib/assets.ts
```
